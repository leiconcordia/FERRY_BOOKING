CREATE TABLE Users (
    UserID INT IDENTITY(1,1) PRIMARY KEY,
    FirstName VARCHAR(100) NOT NULL,
    LastName VARCHAR(100) NOT NULL,
    CompanyName VARCHAR(200) NOT NULL,
    Email VARCHAR(200) NOT NULL UNIQUE,
    PasswordHash VARBINARY(MAX) NOT NULL,
    Role VARCHAR(20) NOT NULL DEFAULT 'FerryOwner',
    DateCreated DATETIME NOT NULL DEFAULT GETDATE(),
    IsActive BIT NOT NULL DEFAULT 1
);

CREATE TABLE Ferry (
    FerryID INT IDENTITY(1,1) PRIMARY KEY,
    FerryCode VARCHAR(50) NOT NULL UNIQUE,
    OwnerID INT NOT NULL,
    FerryName VARCHAR(100) NOT NULL,
    TotalFloors INT NOT NULL,
    TotalCapacity INT NOT NULL DEFAULT 0,
    Status VARCHAR(20) NOT NULL DEFAULT 'Inactive',
    DateRegistered DATETIME DEFAULT GETDATE()
);

CREATE TABLE FerryFloor (
    FloorID INT IDENTITY(1,1) PRIMARY KEY,
    FerryID INT NOT NULL,
    FloorNumber INT NOT NULL,
    Rows INT NOT NULL,
    Columns INT NOT NULL,
    Capacity AS (Rows * Columns) PERSISTED,
    FOREIGN KEY (FerryID) REFERENCES Ferry(FerryID)
);

CREATE TABLE Route (
    RouteID INT IDENTITY(1,1) PRIMARY KEY,
    Origin VARCHAR(100) NOT NULL,
    Destination VARCHAR(100) NOT NULL,
    Distance DECIMAL(10,2) NULL,      -- in kilometers (e.g. 70.50)
    Duration TIME NULL,               -- estimated travel duration (e.g. 02:30:00)
    FerryID INT;
    FOREIGN KEY (FerryID) REFERENCES Ferry(FerryID);
    CONSTRAINT UQ_Route UNIQUE(Origin, Destination)
);

ALTER TABLE Route
ADD FerryID INT;

ALTER TABLE Route
ADD CONSTRAINT FK_Route_Ferry FOREIGN KEY (FerryID)
REFERENCES Ferry(FerryID);


CREATE TABLE Trip (
    TripID INT IDENTITY(1,1) PRIMARY KEY,
    ScheduleID INT NULL,
    FerryID INT NOT NULL,
    RouteID INT NOT NULL,
    TripDate DATE NOT NULL,
    DepartureTime TIME NOT NULL,
    ArrivalTime TIME NOT NULL,
    DaysOfWeek VARCHAR(50) NOT NULL,  -- e.g., 'Mon,Wed,Fri'
    StartDate DATE NOT NULL,
    EndDate DATE NOT NULL,
    TripStatus VARCHAR(20) NOT NULL DEFAULT 'Active', -- Added TripStatus column

    FOREIGN KEY (ScheduleID) REFERENCES Schedule(ScheduleID),
    FOREIGN KEY (FerryID) REFERENCES Ferry(FerryID),
    FOREIGN KEY (RouteID) REFERENCES Route(RouteID)
);


CREATE TABLE FerryDocuments (
    DocumentID INT IDENTITY(1,1) PRIMARY KEY,
    FerryID INT NOT NULL FOREIGN KEY REFERENCES Ferry(FerryID),
    COFile VARBINARY(MAX) NULL,
    VRFile VARBINARY(MAX) NULL,
    SCFile VARBINARY(MAX) NULL,
    IDFile VARBINARY(MAX) NULL,
    POFile VARBINARY(MAX) NULL,
    FPFile VARBINARY(MAX) NULL
);

CREATE TABLE BookedSeats
(
    BookedSeatID INT IDENTITY(1,1) PRIMARY KEY,
    TripID INT NOT NULL,
    FloorID INT NOT NULL,
    FerryID INT NOT NULL,
    SeatNumber VARCHAR(10) NULL,   -- optional, if you track exact seat
    BookingDate DATETIME DEFAULT GETDATE(),
    FOREIGN KEY (TripID) REFERENCES Trip(TripID),
    FOREIGN KEY (FerryID) REFERENCES Ferry(FerryID)
);

CREATE TABLE FerryRoute (
    FerryRouteID INT IDENTITY(1,1) PRIMARY KEY,
    FerryID INT NOT NULL,
    RouteID INT NOT NULL,
    FOREIGN KEY (FerryID) REFERENCES Ferry(FerryID),
    FOREIGN KEY (RouteID) REFERENCES Route(RouteID),
    CONSTRAINT UQ_FerryRoute UNIQUE(FerryID, RouteID)
);

CREATE TABLE TripFloorPrice (
    TripFloorPriceID INT IDENTITY(1,1) PRIMARY KEY,
    TripID INT NOT NULL,
    FerryID INT NOT NULL,
    FloorNumber INT NOT NULL,
    Price DECIMAL(10,2) NOT NULL,

    FOREIGN KEY (TripID) REFERENCES Trip(TripID),
    FOREIGN KEY (FerryID, FloorNumber) REFERENCES FerryFloor(FerryID, FloorNumber)
);
CREATE TABLE Schedule (
    ScheduleID INT IDENTITY(1,1) PRIMARY KEY,
    FerryID INT NOT NULL,
    RouteID INT NOT NULL,
    DepartureTime TIME NOT NULL,
    ArrivalTime TIME NOT NULL,
    DaysOfWeek VARCHAR(100) NOT NULL,   -- e.g. 'Mon,Tue,Wed,Thu,Fri'
    StartDate DATE NOT NULL DEFAULT GETDATE(),
    EndDate DATE NULL,                 -- null = ongoing
    IsActive BIT NOT NULL DEFAULT 1,
    FOREIGN KEY (FerryID) REFERENCES Ferry(FerryID),
    FOREIGN KEY (RouteID) REFERENCES Route(RouteID)
);

CREATE TABLE Booking
(
    BookingID     bigint         identity(1,1)  primary key,
    BookingRef    varchar(20)    not null unique,
    TripID        int            not null foreign key references Trip(TripID),
    BookingDate   datetime       default getdate(),
    TotalAmount   money          not null
);

CREATE TABLE BookingPassenger
(
    PassengerID   bigint         identity(1,1)  primary key,
    BookingID     bigint         not null foreign key references Booking(BookingID),
    SeatCode      varchar(20)    not null,
    FullName      nvarchar(200)  not null,
    Age           int            not null,
    Gender        varchar(10)    not null,
    Discount      varchar(20)    null,
    Price         money          not null,
    IDImage       varbinary(max) null
    ContactNumber NVARCHAR(50) NOT NULL DEFAULT('');
    ValidID VARBINARY(MAX) NULL;

);

    CREATE TABLE TripCancellation (
        CancellationID INT IDENTITY(1,1) PRIMARY KEY,
        TripID INT NOT NULL,
        FerryID INT NOT NULL,
        CancellationReason NVARCHAR(500) NOT NULL,
        CancelledBy INT NOT NULL, -- UserID (OwnerID)
        CancellationDate DATETIME NOT NULL DEFAULT GETDATE(),
        TotalPassengersAffected INT NOT NULL DEFAULT 0,
        TotalRefundAmount DECIMAL(18,2) NOT NULL DEFAULT 0,
        NotificationStatus VARCHAR(50) NOT NULL DEFAULT 'Pending', -- Pending, Sent, Failed
        FOREIGN KEY (TripID) REFERENCES Trip(TripID),
        FOREIGN KEY (FerryID) REFERENCES Ferry(FerryID),
        FOREIGN KEY (CancelledBy) REFERENCES Users(UserID)
        );

-- First, add TripStatus column to Trip table
ALTER TABLE Trip
ADD TripStatus VARCHAR(20) NOT NULL DEFAULT 'Active';

-- Create BookingRefund table (refined version)
CREATE TABLE BookingRefund (
    RefundID BIGINT IDENTITY(1,1) PRIMARY KEY,
    BookingID BIGINT NOT NULL,
    CancellationID INT NOT NULL,
    RefundAmount DECIMAL(18,2) NOT NULL,
    RefundStatus VARCHAR(50) NOT NULL DEFAULT 'Pending', -- Pending, Processed, Completed, Failed
    RefundMethod VARCHAR(50) NULL, -- Cash, Bank Transfer, GCash, etc.
    RefundDate DATETIME NULL,
    ProcessedBy INT NULL, -- UserID who processed the refund
    ProcessedDate DATETIME NULL,
    Notes NVARCHAR(500) NULL,
    FOREIGN KEY (BookingID) REFERENCES Booking(BookingID),
    FOREIGN KEY (CancellationID) REFERENCES TripCancellation(CancellationID),
    FOREIGN KEY (ProcessedBy) REFERENCES Users(UserID)
);

-- Create PassengerNotification table (refined version)
CREATE TABLE PassengerNotification (
    NotificationID BIGINT IDENTITY(1,1) PRIMARY KEY,
    BookingID BIGINT NOT NULL,
    CancellationID INT NULL,
    NotificationType VARCHAR(50) NOT NULL, -- Cancellation, Refund, Confirmation, etc.
    RecipientEmail NVARCHAR(200) NULL,
    RecipientName NVARCHAR(200) NOT NULL,
    Subject NVARCHAR(200) NOT NULL,
    MessageBody NVARCHAR(MAX) NOT NULL,
    SentDate DATETIME NULL,
    SentStatus VARCHAR(50) NOT NULL DEFAULT 'Pending', -- Pending, Sent, Failed
    ErrorMessage NVARCHAR(500) NULL,
    RetryCount INT NOT NULL DEFAULT 0,
    FOREIGN KEY (BookingID) REFERENCES Booking(BookingID),
    FOREIGN KEY (CancellationID) REFERENCES TripCancellation(CancellationID)
);

-- Add index for better performance
CREATE INDEX IX_Trip_TripStatus ON Trip(TripStatus);
CREATE INDEX IX_Trip_TripDate ON Trip(TripDate);
CREATE INDEX IX_BookingRefund_CancellationID ON BookingRefund(CancellationID);


//My ferries table

CREATE OR ALTER VIEW vw_FerryDisplay AS
SELECT 
    f.FerryID,
    f.FerryCode,
    f.FerryName,
    f.TotalCapacity AS Capacity,
    f.Status,
    f.OwnerID,

    -- SEAT INFO PER FLOOR (without Price)
    STUFF((
        SELECT CHAR(10) + 'Floor ' + CAST(ff.FloorNumber AS VARCHAR(5)) + 
               ' ' + CAST(ff.Rows AS VARCHAR(5)) + 'x' + CAST(ff.Columns AS VARCHAR(5))
        FROM FerryFloor ff
        WHERE ff.FerryID = f.FerryID
        ORDER BY ff.FloorNumber
        FOR XML PATH(''), TYPE
    ).value('.', 'NVARCHAR(MAX)'), 1, 1, '') AS Seats,

    -- ROUTE ASSIGNED FROM FerryRoute TABLE
    ISNULL((
        SELECT TOP 1 
            r.Origin + ' <--> ' + r.Destination
        FROM FerryRoute fr
        INNER JOIN Route r ON r.RouteID = fr.RouteID
        WHERE fr.FerryID = f.FerryID
        ORDER BY fr.FerryRouteID ASC
    ), 'No trips / No route') AS Route

FROM Ferry f;


        
     

     //Search Ferry view

CREATE OR ALTER VIEW vw_FerrySearch
AS
SELECT
    t.TripID,
    f.FerryID,
    u.CompanyName      AS Company,
    f.FerryName        AS Ferry,
    CONCAT(r.Origin,' -> ',r.Destination) AS Route,
    CAST(t.TripDate AS date) AS TripDate,
    t.DepartureTime,
    t.ArrivalTime,
    f.TripStatus           AS FerryStatus,

    /* existing seat counts */
    ISNULL((SELECT SUM(capacity) FROM FerryFloor ff WHERE ff.FerryID = f.FerryID),0) AS TotalSeats,
    ISNULL((SELECT COUNT(*)      FROM BookedSeats bs WHERE bs.TripID = t.TripID),0)   AS BookedSeats,
    ISNULL((SELECT SUM(capacity) FROM FerryFloor ff WHERE ff.FerryID = f.FerryID),0)
    - ISNULL((SELECT COUNT(*)    FROM BookedSeats bs WHERE bs.TripID = t.TripID),0)   AS AvailableSeats,

STUFF((
    SELECT CHAR(13)+CHAR(10) + CONCAT('Floor ', ff.FloorNumber,
                                      ': ', booked.Booked,'/',ff.Capacity,
                                      ' Price : ', FORMAT(tfp.Price,'N0'))
    FROM FerryFloor ff
    JOIN TripFloorPrice tfp
         ON tfp.FerryID   = ff.FerryID
        AND tfp.FloorNumber = ff.FloorNumber
        AND tfp.TripID    = t.TripID
    CROSS APPLY (
        SELECT COUNT(*) AS Booked
        FROM BookedSeats bs
        WHERE bs.TripID  = t.TripID
          AND bs.FerryID = ff.FerryID
          AND LEFT(bs.SeatNumber,1) = CAST(ff.FloorNumber AS varchar(1))
    ) booked
    WHERE ff.FerryID = f.FerryID
    ORDER BY ff.FloorNumber
    FOR XML PATH(''), TYPE
).value('.','nvarchar(max)'), 2, 0, '') AS FloorPrices

FROM Trip t
JOIN Ferry  f ON f.FerryID = t.FerryID
JOIN Users  u ON u.UserID  = f.OwnerID

JOIN Route  r ON r.RouteID = t.RouteID;


//Ferry Schedule
CREATE OR ALTER VIEW vw_TripScheduleWithPrices AS
SELECT
    s.ScheduleID,
    s.FerryID,
    f.FerryName,
    s.RouteID,
    r.Origin + ' -> ' + r.Destination AS Route,
    s.DepartureTime,
    s.ArrivalTime,
    s.DaysOfWeek,
    s.StartDate,
    s.EndDate,
    f.OwnerID,
    s.IsActive,
    -- Get floor prices from any trip belonging to this schedule
    (
        SELECT STRING_AGG(
            CONCAT('Floor ', ff.FloorNumber, ': P', FORMAT(tfp.Price, 'N2')), 
            CHAR(13) + CHAR(10)
        )
        WITHIN GROUP (ORDER BY ff.FloorNumber)
        FROM FerryFloor ff
        LEFT JOIN TripFloorPrice tfp 
            ON tfp.FerryID = ff.FerryID 
            AND tfp.FloorNumber = ff.FloorNumber
            AND tfp.TripID = (
                -- Get the first trip from this schedule
                SELECT TOP 1 TripID 
                FROM Trip 
                WHERE ScheduleID = s.ScheduleID
                ORDER BY TripDate
            )
        WHERE ff.FerryID = s.FerryID
    ) AS Price
FROM Schedule s
JOIN Ferry f ON s.FerryID = f.FerryID
JOIN Route r ON s.RouteID = r.RouteID
WHERE s.IsActive = 1;

CREATE OR ALTER VIEW vw_RouteDisplay AS
SELECT 
    r.RouteID AS RouteID,           -- real RouteID
    fr.FerryRouteID AS FerryRouteID,  -- mapping ID (edit/delete)
    f.FerryID,
    f.FerryName AS Ferry,
    r.Origin + ' -> ' + r.Destination AS Route,
    r.Distance,
    r.Duration,
    f.OwnerID
FROM FerryRoute fr
INNER JOIN Ferry f ON fr.FerryID = f.FerryID
INNER JOIN Route r ON fr.RouteID = r.RouteID;


CREATE OR ALTER VIEW vw_BookingSummary AS
SELECT 
    f.FerryName,
    f.OwnerID,
    r.Origin + ' -> ' + r.Destination AS Routes,
    t.TripDate,
    FORMAT(t.DepartureTime, 'h:mm tt') + ' to ' + FORMAT(t.ArrivalTime, 'h:mm tt') AS Schedule,
    ISNULL(BookedCount.Booked, 0) AS Booked,
    (f.TotalCapacity - ISNULL(BookedCount.Booked, 0)) AS Available,
    CASE 
       WHEN f.TotalCapacity > 0 THEN 
            LTRIM(STR(ROUND(ISNULL(BookedCount.Booked, 0) * 100.0 / f.TotalCapacity, 1), 6, 1)) + ' %'
       ELSE '0 %'
    END AS Occupancy,
    ISNULL(Rev.TotalRevenue, 0) AS Revenue
FROM Trip t
JOIN Ferry f ON t.FerryID = f.FerryID
JOIN Route r ON t.RouteID = r.RouteID
LEFT JOIN (
    SELECT bs.TripID, COUNT(*) AS Booked
    FROM BookedSeats bs
    GROUP BY bs.TripID
) AS BookedCount ON BookedCount.TripID = t.TripID
LEFT JOIN (
    SELECT b.TripID, SUM(b.TotalAmount) AS TotalRevenue
    FROM Booking b
    GROUP BY b.TripID
) AS Rev ON Rev.TripID = t.TripID
WHERE t.ArrivalTime < GETDATE();


CREATE OR ALTER VIEW vw_TodaysTripDashboard AS
SELECT
    t.TripID,
    t.FerryID,
    f.FerryName,
    f.OwnerID,
    r.Origin + ' to ' + r.Destination AS Route,
    t.TripDate,
    t.DepartureTime,
    t.ArrivalTime,
    -- Concatenate floor info: FloorNumber: Booked/Capacity
    STUFF((
        SELECT ', ' + 'Floor ' + CAST(ff.FloorNumber AS VARCHAR) + ': ' 
               + CAST(ISNULL(bs.BookedCount, 0) AS VARCHAR) + '/' + CAST(ff.Capacity AS VARCHAR)
        FROM FerryFloor ff
        LEFT JOIN (
            SELECT LEFT(SeatNumber,1) AS FloorNum, COUNT(*) AS BookedCount
            FROM BookedSeats
            WHERE TripID = t.TripID AND FerryID = t.FerryID
            GROUP BY LEFT(SeatNumber,1)
        ) bs ON CAST(ff.FloorNumber AS VARCHAR) = bs.FloorNum
        WHERE ff.FerryID = t.FerryID
        ORDER BY ff.FloorNumber
        FOR XML PATH(''), TYPE).value('.', 'NVARCHAR(MAX)'), 1, 2, '') AS FloorBreakdown
FROM Trip t
JOIN Ferry f ON t.FerryID = f.FerryID
JOIN Route r ON t.RouteID = r.RouteID
WHERE CAST(t.TripDate AS DATE) = CAST(GETDATE() AS DATE)
  AND f.OwnerID = 1;  -- Replace 1 with desired OwnerID
